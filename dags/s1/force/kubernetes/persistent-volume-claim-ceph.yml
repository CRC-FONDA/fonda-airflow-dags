apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: force-pvc-default
  namespace: default
spec:
  storageClassName: cephfs
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 100Gi

