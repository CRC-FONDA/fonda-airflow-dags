apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: force-pvc
  namespace: default
spec:
  storageClassName: cephfs
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 750Gi

